# The committed pnpm-lock.yaml — not the version range — is the reproducibility
# contract (NFR-08). engine-strict off so the prototype installs on newer Node
# while .nvmrc/engines document the pinned target (Node 22).
engine-strict=false
